meta {
  name: Delete Category
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/categories/{{category_id}}?user_id={{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
