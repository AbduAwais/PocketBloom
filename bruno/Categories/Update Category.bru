meta {
  name: Update Category
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/categories/{{category_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Food and Groceries",
    "is_active": true
  }
}
